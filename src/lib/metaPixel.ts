/** Meta Pixel is now loaded directly in index.html. This function is a no-op kept for compatibility. */
export function initMetaPixel() {
  // Pixel script is injected via index.html with hardcoded ID 1999229120989625
  // Nothing to do here
}
